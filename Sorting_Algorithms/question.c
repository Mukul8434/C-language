/*Ques:-Given an array of integers with 1 to n elements and the size of the array is n+1.one element is occuring more than once i.e,duplicate number is present.Find the duplicate element.*/
#include<stdio.h>
#include<string.h>
int main(){
    
    return 0;
}